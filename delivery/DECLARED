[
  {
    "scope": "author:briananderson1222 branch-prefix:feat/owner-queue-and-evidence-graph",
    "reason": "PR for issues #273 + #274: the owner-verb queue as the default surface, and evidence nodes with interpretation-first panels in the process-flow graph. Agent-delivered outside a bundled workflow, so no trust bundle exists to reconcile; this file is new in this repo and carries the same governed no-agent-delivery marker contract as its siblings. Verification in lieu of a bundle: root and per-workspace typechecks, console-core 89, console-server 487, console-ui 347 tests and the browser suite (63 passed / 1 pre-existing conditional skip) all green on the committed tree, run independently by the implementer, a fresh-context reviewer, and a delta reviewer; content-boundary green. Review history: round 1 CHANGES_REQUESTED with a reproduced HIGH - the dangling-claim detector derived absence from a slice(0,6)-truncated render set, fabricating a false 'No evidence recorded' node for a claim whose only evidence fell past the cap - plus a reproduced cross-workflow raw-id join collision relaying the wrong producer's text, caps silently dropping dead nodes, and a computed-looking 0 rendered with no reports feed. All four fixed with revert-proven pins (verified red on the pre-fix parent). Delta review: CLEAR TO MERGE, and its one substantive finding fixed in the final commit - a bare evidenceRef suppressed the dead node (a pointer counted as evidence; fabricated presence, the inverse of the HIGH), now requiring the ref target to resolve in state. DISCLOSED PLAINLY: the #274 renderer is DARK CODE - neither ProcessFlowDiagram nor NodeDetailDrawer is mounted by App on main or on this branch; the only live surface change from #274 is the Operate node-count receipt; mounting is deliberate follow-up work. The queue's pulse renders its own reconciled counts, not the flow-agents stop-gate record (#1266) the issue names as the eventual producer. Remaining accepted gaps filed as #275 (gates-cap indicator, mixed flat/qualified-id join posture, perf shape, indicator-id collisions, indicator copy, word-budget enumeration coverage). Scoped to this author and branch only.",
    "approved_by": "brian.anderson1222 (2026-08-17 session: 'do it.. drive all to batched merge' following 'each of those issues needs a fresh pair of eyes before pickup')",
    "declared_at": "2026-08-17T09:10:00Z"
  }
]
